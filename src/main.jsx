import React from "react";
import ReactDOM from "react-dom/client";
import AITerminalAgent from "./AITerminalAgent.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AITerminalAgent />
  </React.StrictMode>
);
